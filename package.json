{
  "name": "hk-secondary-school-planner",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "export": "next build",
    "build:ghpages": "env -u ZEABUR -u ZEABUR_USER_ID -u ZEABUR_SERVICE_ID -u ZEABUR_WEB_URL -u ZEABUR_WEB_DOMAIN -u ZEABUR_ENVIRONMENT_ID -u ZEABUR_REGION -u ZEABUR_PROJECT_ID NODE_ENV=production next build",
    "build:zeabur": "env -u ZEABUR -u ZEABUR_USER_ID -u ZEABUR_SERVICE_ID -u ZEABUR_WEB_URL -u ZEABUR_WEB_DOMAIN -u ZEABUR_ENVIRONMENT_ID -u ZEABUR_REGION -u ZEABUR_PROJECT_ID ZEABUR=1 next build",
    "start": "npx serve@latest out -l 3000",
    "serve": "npx serve@latest out -l 3000"
  },
  "dependencies": {
    "next": "^14.2.33",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.39",
    "tailwindcss": "^3.4.6",
    "typescript": "^5.5.3"
  }
}
